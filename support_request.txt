Здравствуйте!

У меня VPS-сервер: 138.16.226.236 (IP)

Мне необходимо подключиться к внешнему API-сервису, который работает исключительно по протоколу IPv6 (у домена нет IPv4-записи, только IPv6 AAAA-запись).

Текущее состояние:
- IPv6 включён в системе (net.ipv6.conf.all.disable_ipv6 = 0)
- Но глобальный IPv6-адрес не назначен — есть только link-local (fe80::/64)
- Исходящие IPv6-соединения не работают: ping6 возвращает "Network is unreachable"

Что нужно:
Предоставить глобальный IPv6-адрес для моего сервера, чтобы он мог делать исходящие IPv6-соединения к внешним ресурсам.

Это необходимо для работы Telegram Mini App, который общается с API Яндекс Такси (fleet.taxi.yandex.net — работает только по IPv6).

Буду благодарен за помощь!
